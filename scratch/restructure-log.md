# Restructure Log

Audit trail for project-restructure runs against `/Users/james/Documents/Claude/Projects/Oscillitron`.

## 2026-05-17 — initial bootstrap

Project was effectively empty (only a Claude-authored `CLAUDE.md` stub existed). No file moves; bootstrap only.

**Created (folders):**
- `inputs/` (+ `.gitkeep`)
- `references/` (+ `.gitkeep`)
- `artifacts/` (+ `.gitkeep`)
- `scratch/` (+ `.gitkeep`)
- `skills/` (+ `.gitkeep`)

**Created (files):**
- `INDEX.md` — skeleton, empty sections for References / Skills / Key artifacts.
- `scratch/restructure-log.md` — this file.

**Modified:**
- `CLAUDE.md` — augmented with the `CLAUDE GENERATED` marker, Provenance rule, Folder conventions, and Default behaviors sections at the top. All pre-existing TBD scaffolding (Status, Stack & tooling, Layout, run/build/test, Conventions, Open questions, Notes for Claude) preserved verbatim below a `---` separator.

**Moved:** none.
**Ambiguous / unresolved:** none.
**`inputs/` candidates flagged for user:** none.
